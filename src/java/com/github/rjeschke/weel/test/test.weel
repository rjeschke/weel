
clazz = {
};

sub clazz:ctor(args)
    println("Constructor.");
    this->args = args;
end

sub clazz:print()
    println("Hello "..this->args.."!");
end

ins = new(clazz, "world");

ins->print();

sb = new(java.lang.StringBuilder);
sb->append("Hello");
sb->append(" world!");
println(sb->toString());
